#!/bin/sh

sudo systemctl start bluetooth
echo -e 'power on\nconnect 1C:1A:C0:E7:06:02\nquit' | bluetoothctl
xset m 0 0  # Disable acceleration
# This is for the vertical scrolling, cluncky but works
sudo rmmod hid_magicmouse
sudo modprobe hid_magicmouse scroll-speed=45 scroll-acceleration=1
