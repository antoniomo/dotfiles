#!/bin/sh

export XRANDR_PRIMARY=`xrandr | grep "primary" | cut -d' ' -f1`
export XRANDR_SECONDARY=`xrandr | grep " connected" | grep -v "primary" | cut -d' ' -f1`

if [ -z "$XRANDR_PRIMARY" ]; then
  XRANDR_PRIMARY="$XRANDR_SECONDARY"
fi
